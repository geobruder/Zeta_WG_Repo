{
  "nbformat": 4,
  "nbformat_minor": 5,
  "metadata": {
    "kernelspec": {
      "display_name": "Python 3",
      "language": "python",
      "name": "python3"
    },
    "language_info": {
      "name": "python",
      "version": "3.10"
    }
  },
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {},
      "source": [
        "# County Childhood Vaccination Uptake — analysis_updated_full.ipynb\n",
        "\n",
        "This notebook includes the final robustness additions: artifact generation, SHAP CSV exports, residual diagnostics, hyperparameter summary export, and README creation. Run top→bottom after restarting the kernel."
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 0. Environment and imports\n",
        "import os, sys, json, time, subprocess\n",
        "from pathlib import Path\n",
        "ROOT = Path('.').resolve()\n",
        "DATA_RAW = ROOT / 'data_raw'\n",
        "DATA_CLEAN = ROOT / 'data_clean'\n",
        "MODELS_DIR = ROOT / 'models'\n",
        "OUTPUTS_DIR = ROOT / 'outputs'\n",
        "FIG_DIR = ROOT / 'figures'\n",
        "for d in [DATA_RAW, DATA_CLEAN, MODELS_DIR, OUTPUTS_DIR, FIG_DIR]:\n",
        "    d.mkdir(parents=True, exist_ok=True)\n",
        "\n",
        "# Flags and reproducibility\n",
        "CONFIRM_HEAVY_RUN = False  # set True to run heavy GPU experiments\n",
        "RANDOM_STATE = 42\n",
        "os.environ['PYTHONHASHSEED'] = str(RANDOM_STATE)\n",
        "\n",
        "import numpy as np\n",
        "import pandas as pd\n",
        "import joblib\n",
        "import hashlib\n",
        "from time import perf_counter\n",
        "\n",
        "from sklearn.model_selection import KFold, RandomizedSearchCV, train_test_split\n",
        "from sklearn.pipeline import Pipeline\n",
        "from sklearn.impute import SimpleImputer\n",
        "from sklearn.preprocessing import StandardScaler, OneHotEncoder\n",
        "from sklearn.compose import ColumnTransformer\n",
        "from sklearn.linear_model import ElasticNet\n",
        "from sklearn.ensemble import RandomForestRegressor, GradientBoostingRegressor\n",
        "from sklearn.metrics import mean_squared_error, mean_absolute_error, r2_score\n",
        "\n",
        "try:\n",
        "    import lightgbm as lgb\n",
        "    LGB_AVAILABLE = True\n",
        "except Exception:\n",
        "    LGB_AVAILABLE = False\n",
        "try:\n",
        "    import xgboost as xgb\n",
        "    XGB_AVAILABLE = True\n",
        "except Exception:\n",
        "    XGB_AVAILABLE = False\n",
        "\n",
        "try:\n",
        "    import torch\n",
        "    TORCH_AVAILABLE = True\n",
        "except Exception:\n",
        "    TORCH_AVAILABLE = False\n",
        "\n",
        "if TORCH_AVAILABLE:\n",
        "    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\n",
        "else:\n",
        "    device = 'cpu'\n",
        "\n",
        "try:\n",
        "    import psutil\n",
        "    PSUTIL_AVAILABLE = True\n",
        "except Exception:\n",
        "    PSUTIL_AVAILABLE = False\n",
        "\n",
        "print('Environment ready. PyTorch:', TORCH_AVAILABLE, 'device:', device, 'LightGBM:', LGB_AVAILABLE, 'XGBoost:', XGB_AVAILABLE)"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# SHAP configuration (tweakable)\n",
        "SHAP_CONFIG = {\n",
        "    'tree_sample': 1000,\n",
        "    'linear_sample': 1000,\n",
        "    'mlp_background': 100,\n",
        "    'mlp_sample': 200,\n",
        "    'kernel_nsamples': 200\n",
        "}\n",
        "\n",
        "def write_json_atomic(obj, path):\n",
        "    tmp = Path(str(path) + '.tmp')\n",
        "    with open(tmp, 'w', encoding='utf-8') as f:\n",
        "        json.dump(obj, f, indent=2)\n",
        "    tmp.replace(path)\n",
        "\n",
        "print('SHAP_CONFIG and utilities ready')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# Provenance helpers and run_provenance capture (guarded R capture)\n",
        "def checksum_file(path):\n",
        "    h = hashlib.sha256()\n",
        "    with open(path, 'rb') as f:\n",
        "        for chunk in iter(lambda: f.read(8192), b''):\n",
        "            h.update(chunk)\n",
        "    return h.hexdigest()\n",
        "\n",
        "def record_provenance_dataset(path, source_url='', notes=''):\n",
        "    meta_path = ROOT / 'data_vintages.csv'\n",
        "    row = {\n",
        "        'dataset': Path(path).name,\n",
        "        'path': str(path),\n",
        "        'retrieval_date': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime()),\n",
        "        'checksum': checksum_file(path) if Path(path).exists() else '',\n",
        "        'source_url': source_url,\n",
        "        'notes': notes\n",
        "    }\n",
        "    if meta_path.exists():\n",
        "        dfm = pd.read_csv(meta_path)\n",
        "        dfm = pd.concat([dfm, pd.DataFrame([row])], ignore_index=True)\n",
        "    else:\n",
        "        dfm = pd.DataFrame([row])\n",
        "    dfm.to_csv(meta_path, index=False)\n",
        "    return row\n",
        "\n",
        "def capture_run_provenance(outpath=OUTPUTS_DIR / 'run_provenance.json'):\n",
        "    try:\n",
        "        pip_freeze = subprocess.check_output([sys.executable, '-m', 'pip', 'freeze']).decode().splitlines()\n",
        "    except Exception:\n",
        "        pip_freeze = []\n",
        "    try:\n",
        "        r_cmd = [\n",
        "            'Rscript',\n",
        "            '-e',\n",
        "            \"if(requireNamespace('jsonlite', quietly=TRUE)) cat(jsonlite::toJSON(sessionInfo(), auto_unbox=TRUE)) else cat('')\"\n",
        "        ]\n",
        "        r_info = subprocess.check_output(r_cmd, stderr=subprocess.DEVNULL, timeout=20).decode().strip()\n",
        "        if r_info == '':\n",
        "            r_info = None\n",
        "    except Exception:\n",
        "        r_info = None\n",
        "\n",
        "    run_info = {\n",
        "        'timestamp': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime()),\n",
        "        'git_sha': (subprocess.check_output(['git','rev-parse','HEAD']).decode().strip() if Path('.git').exists() else ''),\n",
        "        'python_version': sys.version.replace('\\n',' '),\n",
        "        'pip_freeze': pip_freeze,\n",
        "        'r_session_info_json': r_info,\n",
        "        'random_state': RANDOM_STATE,\n",
        "        'torch_available': TORCH_AVAILABLE,\n",
        "    }\n",
        "    if TORCH_AVAILABLE:\n",
        "        run_info['torch_version'] = torch.__version__\n",
        "        run_info['cuda_available'] = torch.cuda.is_available()\n",
        "        if torch.cuda.is_available():\n",
        "            run_info['cuda_device_count'] = torch.cuda.device_count()\n",
        "            try:\n",
        "                run_info['cuda_device_name'] = torch.cuda.get_device_name(torch.cuda.current_device())\n",
        "            except Exception:\n",
        "                run_info['cuda_device_name'] = ''\n",
        "    write_json_atomic(run_info, outpath)\n",
        "    write_json_atomic(run_info, MODELS_DIR / 'env_info.json')\n",
        "    return run_info\n",
        "\n",
        "print('Provenance helpers ready (R capture guarded)')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# Ingest raw files and build analysis_table.csv (robust checks)\n",
        "cdc_file = DATA_RAW / 'cdc_vaxview_fhky-rtsk_mmr_19m.csv'\n",
        "acs_file = DATA_RAW / 'acs_2021_counties_merged_validated.csv'\n",
        "covidcast_file = DATA_RAW / 'covidcast_fb-survey_smoothed_vaccinate_children_county_20210604.csv'\n",
        "\n",
        "import re\n",
        "def pad_fips_val(x):\n",
        "    if x is None:\n",
        "        return ''\n",
        "    s = str(x).strip()\n",
        "    digits = re.sub(r'\\D', '', s)\n",
        "    if digits == '':\n",
        "        return ''\n",
        "    return digits.zfill(5)\n",
        "\n",
        "def safe_read_csv(path, **kwargs):\n",
        "    if Path(path).exists():\n",
        "        return pd.read_csv(path, dtype=str, **kwargs)\n",
        "    else:\n",
        "        return pd.DataFrame()\n",
        "\n",
        "if not cdc_file.exists() or not acs_file.exists():\n",
        "    print('One or more raw files missing. Please place canonical raw files in data_raw/ or run ingestion cells.')\n",
        "else:\n",
        "    cdc = safe_read_csv(cdc_file)\n",
        "    acs = safe_read_csv(acs_file)\n",
        "    covidcast = safe_read_csv(covidcast_file) if covidcast_file.exists() else pd.DataFrame()\n",
        "    cdc.columns = [c.lower() for c in cdc.columns]\n",
        "    acs.columns = [c.lower() for c in acs.columns]\n",
        "    covidcast.columns = [c.lower() for c in covidcast.columns]\n",
        "    if 'geo_value' in cdc.columns:\n",
        "        cdc['fips'] = cdc['geo_value'].apply(pad_fips_val)\n",
        "    elif 'fips' in cdc.columns:\n",
        "        cdc['fips'] = cdc['fips'].apply(pad_fips_val)\n",
        "    else:\n",
        "        cdc['fips'] = cdc.index.to_series().apply(pad_fips_val)\n",
        "    cov_candidates = ['coverage_estimate', 'value', 'coverage', 'mmr_coverage']\n",
        "    cov_col = next((c for c in cov_candidates if c in cdc.columns), None)\n",
        "    if cov_col is None:\n",
        "        raise KeyError(\"No coverage column found in CDC file. Available columns: \" + \", \".join(cdc.columns))\n",
        "    base = cdc[['fips', cov_col]].rename(columns={cov_col: 'coverage_estimate'})\n",
        "    if 'fips' not in acs.columns and 'state' in acs.columns and 'county' in acs.columns:\n",
        "        acs['state'] = acs['state'].astype(str).str.zfill(2)\n",
        "        acs['county'] = acs['county'].astype(str).str.zfill(3)\n",
        "        acs['fips'] = (acs['state'] + acs['county']).apply(pad_fips_val)\n",
        "    if 'fips' in acs.columns:\n",
        "        acs['fips'] = acs['fips'].apply(pad_fips_val)\n",
        "    merged = base.merge(acs, on='fips', how='left')\n",
        "    if not covidcast.empty and 'geo_value' in covidcast.columns and 'value' in covidcast.columns:\n",
        "        covidcast['fips'] = covidcast['geo_value'].apply(pad_fips_val)\n",
        "        merged = merged.merge(covidcast[['fips','value']].rename(columns={'value':'vaccinate_children'}), on='fips', how='left')\n",
        "    for c in merged.columns:\n",
        "        if c != 'fips':\n",
        "            merged[c] = pd.to_numeric(merged[c], errors='coerce')\n",
        "    analysis_path = DATA_CLEAN / 'analysis_table.csv'\n",
        "    merged.to_csv(analysis_path, index=False)\n",
        "    record_provenance_dataset(analysis_path, source_url='local_merge', notes='merged cdc+acs+covidcast (robust pad_fips_val applied)')\n",
        "    print('Saved analysis_table.csv rows:', len(merged))"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# Preprocessing: build ColumnTransformer and save preprocessor\n",
        "analysis_path = DATA_CLEAN / 'analysis_table.csv'\n",
        "if not analysis_path.exists():\n",
        "    raise RuntimeError('analysis_table.csv missing; run ingestion or place file in data_clean/')\n",
        "df = pd.read_csv(analysis_path)\n",
        "y = df['coverage_estimate'].astype(float).values\n",
        "X = df.drop(columns=['coverage_estimate'])\n",
        "if 'fips' in X.columns:\n",
        "    fips_col = X['fips'].astype(str).values\n",
        "    X = X.drop(columns=['fips'])\n",
        "else:\n",
        "    fips_col = None\n",
        "numeric_cols = X.select_dtypes(include=['number']).columns.tolist()\n",
        "categorical_cols = X.select_dtypes(include=['object','category']).columns.tolist()\n",
        "numeric_transformer = Pipeline([('imputer', SimpleImputer(strategy='median')), ('scaler', StandardScaler())])\n",
        "categorical_transformer = Pipeline([('imputer', SimpleImputer(strategy='most_frequent')), ('onehot', OneHotEncoder(handle_unknown='ignore'))])\n",
        "preprocessor = ColumnTransformer([('num', numeric_transformer, numeric_cols), ('cat', categorical_transformer, categorical_cols)], remainder='drop')\n",
        "preprocessor.fit(X)\n",
        "X_pre = preprocessor.transform(X)\n",
        "joblib.dump(preprocessor, MODELS_DIR / 'preprocessor.joblib')\n",
        "pd.DataFrame(X_pre.toarray() if hasattr(X_pre, 'toarray') else X_pre).to_csv(OUTPUTS_DIR / 'X_preprocessed.csv', index=False)\n",
        "pd.DataFrame({'y': y}).to_csv(OUTPUTS_DIR / 'y.csv', index=False)\n",
        "print('Saved preprocessor and preprocessed arrays')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# Impute numeric features (median) + add missingness indicators, then drop rows with missing target\n",
        "from sklearn.impute import SimpleImputer\n",
        "outputs_dir = OUTPUTS_DIR\n",
        "\n",
        "if hasattr(X_pre, 'toarray'):\n",
        "    X_df = pd.DataFrame(X_pre.toarray())\n",
        "elif isinstance(X_pre, pd.DataFrame):\n",
        "    X_df = X_pre.copy()\n",
        "else:\n",
        "    X_df = pd.DataFrame(np.asarray(X_pre))\n",
        "\n",
        "y_arr = np.asarray(y).ravel()\n",
        "numeric_cols = X_df.select_dtypes(include=[np.number]).columns.tolist()\n",
        "if len(numeric_cols) == 0:\n",
        "    numeric_cols = X_df.columns.tolist()\n",
        "\n",
        "for c in numeric_cols:\n",
        "    X_df[f'{c}_missing'] = X_df[c].isna().astype(int)\n",
        "\n",
        "imp = SimpleImputer(strategy='median')\n",
        "X_df[numeric_cols] = imp.fit_transform(X_df[numeric_cols])\n",
        "joblib.dump(imp, MODELS_DIR / 'simple_imputer_numeric_median.joblib', compress=3)\n",
        "\n",
        "X_arr = X_df.values\n",
        "y_arr = np.asarray(y).ravel()\n",
        "mask_y_finite = ~np.isnan(y_arr) & ~np.isinf(y_arr)\n",
        "mask_X_finite = ~np.any(np.isnan(X_arr) | np.isinf(X_arr), axis=1)\n",
        "keep_mask = mask_y_finite & mask_X_finite\n",
        "n_keep = int(keep_mask.sum())\n",
        "n_drop = int(len(y_arr) - n_keep)\n",
        "if n_drop > 0:\n",
        "    dropped_indices = np.where(~keep_mask)[0].tolist()\n",
        "    pd.DataFrame({'dropped_index': dropped_indices}).to_csv(outputs_dir / 'dropped_rows_after_impute.csv', index=False)\n",
        "\n",
        "summary = {\n",
        "    'original_rows': int(len(y_arr)),\n",
        "    'kept_rows': n_keep,\n",
        "    'dropped_rows': n_drop,\n",
        "    'dropped_reason': {\n",
        "        'y_nan': int(np.isnan(y_arr).sum()),\n",
        "        'y_inf': int(np.isinf(y_arr).sum()),\n",
        "        'x_any_nan_after_impute': int(np.any(np.isnan(X_arr), axis=1).sum()),\n",
        "        'x_any_inf_after_impute': int(np.any(np.isinf(X_arr), axis=1).sum())\n",
        "    },\n",
        "    'numeric_imputer_saved': str(MODELS_DIR / 'simple_imputer_numeric_median.joblib')\n",
        "}\n",
        "with open(outputs_dir / 'data_imputation_and_cleaning_summary.json', 'w') as f:\n",
        "    json.dump(summary, f, indent=2)\n",
        "\n",
        "X_full = X_arr[keep_mask]\n",
        "y_full = y_arr[keep_mask]\n",
        "print(f'Imputation complete. Kept {n_keep} rows; dropped {n_drop} rows. Summary written to {outputs_dir / \"data_imputation_and_cleaning_summary.json\"}')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# Optional: group (state) median imputation with audit (ABC Hotels pattern)\n",
        "analysis_path = DATA_CLEAN / 'analysis_table.csv'\n",
        "if analysis_path.exists():\n",
        "    analysis = pd.read_csv(analysis_path)\n",
        "    if 'state' not in analysis.columns and 'fips' in analysis.columns:\n",
        "        analysis['state'] = analysis['fips'].astype(str).str.zfill(5).str[:2]\n",
        "    num_cols = analysis.select_dtypes(include=[np.number]).columns.tolist()\n",
        "    if 'coverage_estimate' in num_cols:\n",
        "        num_cols.remove('coverage_estimate')\n",
        "    if 'state' in analysis.columns:\n",
        "        group_meds = analysis.groupby('state')[num_cols].median()\n",
        "    else:\n",
        "        group_meds = pd.DataFrame([analysis[num_cols].median()], index=['__global__'])\n",
        "    global_meds = analysis[num_cols].median()\n",
        "    def fill_row_with_group_meds(row):\n",
        "        if 'state' in analysis.columns:\n",
        "            g = row.get('state', None)\n",
        "            if pd.notna(g) and g in group_meds.index:\n",
        "                meds = group_meds.loc[g]\n",
        "            else:\n",
        "                meds = global_meds\n",
        "        else:\n",
        "            meds = global_meds\n",
        "        for c in num_cols:\n",
        "            if pd.isna(row.get(c, np.nan)):\n",
        "                row[c] = meds[c]\n",
        "        return row\n",
        "    analysis_imputed = analysis.copy()\n",
        "    analysis_imputed = analysis_imputed.apply(fill_row_with_group_meds, axis=1)\n",
        "    for c in num_cols:\n",
        "        miss_col = f\"{c}_missing\"\n",
        "        if miss_col not in analysis_imputed.columns:\n",
        "            analysis_imputed[miss_col] = analysis[c].isna().astype(int)\n",
        "    filled_counts = {}\n",
        "    for c in num_cols:\n",
        "        orig_na = int(analysis[c].isna().sum())\n",
        "        new_na = int(analysis_imputed[c].isna().sum())\n",
        "        filled_counts[c] = orig_na - new_na\n",
        "    audit = {\n",
        "        'group_col_used': 'state' if 'state' in analysis.columns else None,\n",
        "        'original_rows': int(len(analysis)),\n",
        "        'numeric_columns': num_cols,\n",
        "        'filled_counts': filled_counts\n",
        "    }\n",
        "    with open(OUTPUTS_DIR / 'group_imputation_audit.json', 'w') as f:\n",
        "        json.dump(audit, f, indent=2)\n",
        "    preproc_path = MODELS_DIR / 'preprocessor.joblib'\n",
        "    if preproc_path.exists():\n",
        "        preprocessor = joblib.load(preproc_path)\n",
        "        X_pre_new = preprocessor.transform(analysis_imputed.drop(columns=['coverage_estimate']))\n",
        "    else:\n",
        "        X_pre_new = analysis_imputed.drop(columns=['coverage_estimate']).select_dtypes(include=[np.number]).to_numpy()\n",
        "    y_new = pd.to_numeric(analysis_imputed['coverage_estimate'], errors='coerce').to_numpy()\n",
        "    mask_keep = ~np.isnan(y_new) & ~np.isinf(y_new)\n",
        "    pd.DataFrame({'dropped_index': np.where(~mask_keep)[0].tolist()}).to_csv(OUTPUTS_DIR / 'dropped_rows_group_impute.csv', index=False)\n",
        "    X_full = np.asarray(X_pre_new)[mask_keep]\n",
        "    y_full = y_new[mask_keep]\n",
        "    print('Group imputation applied. Kept rows:', int(mask_keep.sum()), 'Dropped (target NaN):', int((~mask_keep).sum()))\n",
        "else:\n",
        "    print('analysis_table.csv not found; skipping group imputation cell')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# PRE-CV SANITY CHECK (must run before nested CV)\n",
        "import numpy as np, pandas as pd\n",
        "def save_offending_indices(indices, path):\n",
        "    pd.DataFrame({'dropped_index': list(indices)}).to_csv(path, index=False)\n",
        "\n",
        "assert 'X_full' in globals() and 'y_full' in globals(), \"X_full or y_full not defined. Run preprocessing cells first.\"\n",
        "\n",
        "y_full = np.asarray(pd.to_numeric(pd.Series(y_full).to_numpy(), errors='coerce')).ravel().astype(float)\n",
        "Xg = np.asarray(X_full)\n",
        "mask_y = (~np.isnan(y_full)) & (~np.isinf(y_full))\n",
        "mask_X = ~np.any(np.isnan(Xg) | np.isinf(Xg), axis=1)\n",
        "mask_keep = mask_y & mask_X\n",
        "if not mask_keep.all():\n",
        "    dropped = np.where(~mask_keep)[0].tolist()\n",
        "    save_offending_indices(dropped, OUTPUTS_DIR / 'dropped_rows_pre_cv_alignment.csv')\n",
        "    print(f\"Pre-CV check: original rows={len(y_full)}, dropped={len(dropped)}; saved dropped indices to outputs/dropped_rows_pre_cv_alignment.csv\")\n",
        "    X_full = Xg[mask_keep]\n",
        "    y_full = y_full[mask_keep]\n",
        "else:\n",
        "    print(f\"Pre-CV check: original rows={len(y_full)}, no dropped rows.\")\n",
        "\n",
        "assert not np.isnan(y_full).any(), \"y_full still contains NaN after pre-CV alignment.\"\n",
        "assert not np.isinf(y_full).any(), \"y_full still contains Inf after pre-CV alignment.\"\n",
        "assert not np.any(np.isnan(X_full) | np.isinf(X_full)), \"X_full contains NaN/Inf after pre-CV alignment.\"\n",
        "print('Final pre-CV sanity checks passed: no NaN/Inf in X_full or y_full.')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 4. Unified nested CV harness with RandomizedSearchCV inner loop and PyTorch MLP integrated\n",
        "from sklearn.model_selection import RandomizedSearchCV\n",
        "from scipy.stats import uniform, randint\n",
        "\n",
        "outer_cv = KFold(n_splits=5, shuffle=True, random_state=RANDOM_STATE)\n",
        "inner_cv = KFold(n_splits=3, shuffle=True, random_state=RANDOM_STATE)\n",
        "\n",
        "def rmse(y_true, y_pred):\n",
        "    y_true = np.asarray(y_true).ravel().astype(float)\n",
        "    y_pred = np.asarray(y_pred).ravel().astype(float)\n",
        "    return float(np.sqrt(mean_squared_error(y_true, y_pred)))\n",
        "\n",
        "models_summary = {}\n",
        "\n",
        "candidates = {\n",
        "    'elasticnet': {\n",
        "        'estimator': ElasticNet(random_state=RANDOM_STATE, max_iter=5000),\n",
        "        'param_dist': {'alpha': uniform(1e-4, 1.0), 'l1_ratio': uniform(0,1)}\n",
        "    },\n",
        "    'random_forest': {\n",
        "        'estimator': RandomForestRegressor(random_state=RANDOM_STATE, n_jobs=-1),\n",
        "        'param_dist': {\n",
        "            'n_estimators': randint(100,600),\n",
        "            'max_depth': [None, 10, 20, 30],\n",
        "            'max_features': ['sqrt', 'log2', None]\n",
        "        }\n",
        "    },\n",
        "    'gbm': {\n",
        "        'estimator': GradientBoostingRegressor(random_state=RANDOM_STATE),\n",
        "        'param_dist': {'n_estimators': randint(100,600), 'learning_rate': uniform(0.01,0.2), 'max_depth': randint(2,6)}\n",
        "    }\n",
        "}\n",
        "\n",
        "if LGB_AVAILABLE:\n",
        "    candidates['lightgbm'] = {\n",
        "        'estimator': lgb.LGBMRegressor(random_state=RANDOM_STATE, n_jobs=-1),\n",
        "        'param_dist': {\n",
        "            'n_estimators': randint(100,1000),\n",
        "            'learning_rate': uniform(0.01,0.2),\n",
        "            'num_leaves': randint(16,128),\n",
        "            'max_depth': randint(3,12)\n",
        "        }\n",
        "    }\n",
        "\n",
        "if TORCH_AVAILABLE:\n",
        "    from sklearn.base import BaseEstimator, RegressorMixin\n",
        "    class TorchMLPRegressor(BaseEstimator, RegressorMixin):\n",
        "        def __init__(self, hidden_sizes=(128,64), lr=1e-3, weight_decay=1e-4, batch_size=64, epochs=100, patience=10, device=None):\n",
        "            self.hidden_sizes = hidden_sizes\n",
        "            self.lr = lr\n",
        "            self.weight_decay = weight_decay\n",
        "            self.batch_size = batch_size\n",
        "            self.epochs = epochs\n",
        "            self.patience = patience\n",
        "            self.device = device if device is not None else (torch.device('cuda' if torch.cuda.is_available() else 'cpu'))\n",
        "            self._is_fitted = False\n",
        "        def _build_model(self, input_dim):\n",
        "            layers = []\n",
        "            prev = input_dim\n",
        "            for h in self.hidden_sizes:\n",
        "                layers.append(torch.nn.Linear(prev, h))\n",
        "                layers.append(torch.nn.ReLU())\n",
        "                layers.append(torch.nn.Dropout(0.2))\n",
        "                prev = h\n",
        "            layers.append(torch.nn.Linear(prev, 1))\n",
        "            return torch.nn.Sequential(*layers)\n",
        "        def fit(self, X, y):\n",
        "            X = np.asarray(X)\n",
        "            y = np.asarray(y).reshape(-1,1)\n",
        "            device = self.device\n",
        "            self.model_ = self._build_model(X.shape[1]).to(device)\n",
        "            optimizer = torch.optim.Adam(self.model_.parameters(), lr=self.lr, weight_decay=self.weight_decay)\n",
        "            criterion = torch.nn.MSELoss()\n",
        "            ds = torch.utils.data.TensorDataset(torch.tensor(X, dtype=torch.float32), torch.tensor(y, dtype=torch.float32))\n",
        "            loader = torch.utils.data.DataLoader(ds, batch_size=self.batch_size, shuffle=True)\n",
        "            best_loss = float('inf')\n",
        "            epochs_no_improve = 0\n",
        "            for epoch in range(self.epochs):\n",
        "                self.model_.train()\n",
        "                losses = []\n",
        "                for xb, yb in loader:\n",
        "                    xb = xb.to(device); yb = yb.to(device)\n",
        "                    optimizer.zero_grad()\n",
        "                    out = self.model_(xb)\n",
        "                    loss = criterion(out, yb)\n",
        "                    loss.backward(); optimizer.step()\n",
        "                    losses.append(loss.item())\n",
        "                avg = np.mean(losses)\n",
        "                if avg + 1e-6 < best_loss:\n",
        "                    best_loss = avg\n",
        "                    epochs_no_improve = 0\n",
        "                else:\n",
        "                    epochs_no_improve += 1\n",
        "                if epochs_no_improve >= self.patience:\n",
        "                    break\n",
        "            self._is_fitted = True\n",
        "            return self\n",
        "        def predict(self, X):\n",
        "            if not self._is_fitted:\n",
        "                raise RuntimeError('Model not fitted')\n",
        "            X = np.asarray(X)\n",
        "            device = self.device\n",
        "            self.model_.eval()\n",
        "            with torch.no_grad():\n",
        "                out = self.model_(torch.tensor(X, dtype=torch.float32).to(device)).cpu().numpy().reshape(-1)\n",
        "            return out\n",
        "    candidates['mlp_pytorch'] = {\n",
        "        'estimator': TorchMLPRegressor(device=device),\n",
        "        'param_dist': {'hidden_sizes': [(64,32),(128,64)], 'lr': [1e-3, 5e-4], 'weight_decay': [1e-4, 1e-5], 'batch_size': [64,128], 'epochs': [50]}\n",
        "    }\n",
        "\n",
        "def validate_param_space(name, estimator, param_dist):\n",
        "    if isinstance(estimator, RandomForestRegressor):\n",
        "        vals = param_dist.get('max_features', [])\n",
        "        bad = [v for v in vals if isinstance(v, str) and v not in ('sqrt','log2')]\n",
        "        if bad:\n",
        "            raise ValueError(f\"Invalid max_features values for RandomForest: {bad}\")\n",
        "    if LGB_AVAILABLE and hasattr(estimator, '__class__') and estimator.__class__.__name__ == 'LGBMRegressor':\n",
        "        vals = param_dist.get('num_leaves', [])\n",
        "        if isinstance(vals, list) or hasattr(vals, '__iter__'):\n",
        "            bad = [v for v in vals if isinstance(v, (int,)) and v < 2]\n",
        "            if bad:\n",
        "                raise ValueError(f\"Invalid num_leaves for LightGBM: {bad}\")\n",
        "    return True\n",
        "\n",
        "for nm, cfg in candidates.items():\n",
        "    try:\n",
        "        validate_param_space(nm, cfg['estimator'], cfg['param_dist'])\n",
        "    except Exception as e:\n",
        "        raise RuntimeError(f\"Parameter space validation failed for candidate {nm}: {e}\")\n",
        "print('Parameter spaces validated for candidates:', list(candidates.keys()))\n",
        "\n",
        "for name, cfg in candidates.items():\n",
        "    print('\\nCandidate:', name)\n",
        "    est = cfg['estimator']\n",
        "    param_dist = cfg['param_dist']\n",
        "    outer_scores = []\n",
        "    runtimes = []\n",
        "    best_params_per_fold = []\n",
        "    for fold, (train_idx, test_idx) in enumerate(outer_cv.split(X_full), start=1):\n",
        "        X_tr, X_te = X_full[train_idx], X_full[test_idx]\n",
        "        y_tr, y_te = y_full[train_idx], y_full[test_idx]\n",
        "\n",
        "        y_tr = np.asarray(pd.to_numeric(pd.Series(y_tr).to_numpy(), errors='coerce')).ravel().astype(float)\n",
        "        y_te = np.asarray(pd.to_numeric(pd.Series(y_te).to_numpy(), errors='coerce')).ravel().astype(float)\n",
        "\n",
        "        nan_local = np.where(np.isnan(y_tr))[0].tolist()\n",
        "        inf_local = np.where(np.isinf(y_tr))[0].tolist()\n",
        "        if len(nan_local) > 0 or len(inf_local) > 0:\n",
        "            df_dbg = pd.DataFrame(X_tr)\n",
        "            df_dbg['y'] = y_tr\n",
        "            dbg_path = OUTPUTS_DIR / f'debug_offending_rows_candidate_{name}_outer{fold}.csv'\n",
        "            try:\n",
        "                df_dbg.iloc[nan_local + inf_local].to_csv(dbg_path, index=True)\n",
        "                print(f\"Warning: Non-finite targets in training partition for outer fold {fold}. Saved offending rows to {dbg_path}\")\n",
        "            except Exception as e:\n",
        "                print('Warning: could not save offending rows CSV:', e)\n",
        "            keep_mask_local = np.ones(len(y_tr), dtype=bool)\n",
        "            keep_mask_local[nan_local + inf_local] = False\n",
        "            X_tr = X_tr[keep_mask_local]\n",
        "            y_tr = y_tr[keep_mask_local]\n",
        "            train_idx = train_idx[keep_mask_local]\n",
        "\n",
        "        if len(y_tr) < max(10, X_tr.shape[1] + 1):\n",
        "            print(f\"Skipping outer fold {fold} for candidate {name}: insufficient training rows after removing non-finite targets ({len(y_tr)} rows).\")\n",
        "            continue\n",
        "\n",
        "        if name == 'random_forest' and fold == 1:\n",
        "            rs_n_jobs = 1\n",
        "        else:\n",
        "            rs_n_jobs = -1\n",
        "\n",
        "        rs = RandomizedSearchCV(estimator=est,\n",
        "                                param_distributions=param_dist,\n",
        "                                n_iter=12,\n",
        "                                cv=inner_cv,\n",
        "                                scoring='neg_root_mean_squared_error',\n",
        "                                n_jobs=rs_n_jobs,\n",
        "                                random_state=RANDOM_STATE,\n",
        "                                error_score='raise')\n",
        "\n",
        "        t0 = perf_counter()\n",
        "        rs.fit(X_tr, y_tr)\n",
        "        t1 = perf_counter()\n",
        "\n",
        "        best = rs.best_estimator_\n",
        "        best_params_per_fold.append(rs.best_params_)\n",
        "        y_pred = rs.predict(X_te)\n",
        "        y_pred = np.asarray(y_pred).ravel().astype(float)\n",
        "        fold_rmse = rmse(y_te, y_pred)\n",
        "        fold_mae = mean_absolute_error(np.asarray(y_te).ravel().astype(float), y_pred)\n",
        "        fold_r2 = r2_score(np.asarray(y_te).ravel().astype(float), y_pred)\n",
        "        outer_scores.append({'rmse': float(fold_rmse), 'mae': float(fold_mae), 'r2': float(fold_r2)})\n",
        "        runtimes.append(t1 - t0)\n",
        "        print(f' fold {fold}: rmse={fold_rmse:.4f}, time={t1-t0:.1f}s')\n",
        "\n",
        "    if len(outer_scores) == 0:\n",
        "        print(f\"No successful outer folds completed for candidate {name}; skipping summary and final refit.\")\n",
        "        continue\n",
        "\n",
        "    import statistics\n",
        "    summary = {\n",
        "        'model': name,\n",
        "        'cv_rmse_mean': float(statistics.mean([s['rmse'] for s in outer_scores])),\n",
        "        'cv_rmse_std': float(statistics.pstdev([s['rmse'] for s in outer_scores])),\n",
        "        'cv_mae_mean': float(statistics.mean([s['mae'] for s in outer_scores])),\n",
        "        'cv_r2_mean': float(statistics.mean([s['r2'] for s in outer_scores])),\n",
        "        'best_params_per_fold': best_params_per_fold,\n",
        "        'runtime_seconds_mean': float(statistics.mean(runtimes)),\n",
        "        'runtime_seconds_total': float(sum(runtimes))\n",
        "    }\n",
        "\n",
        "    Xg = np.asarray(X_full)\n",
        "    yg = np.asarray(pd.to_numeric(pd.Series(y_full).to_numpy(), errors='coerce')).ravel().astype(float)\n",
        "    mask_keep_global = (~np.isnan(yg)) & (~np.isinf(yg)) & (~np.any(np.isnan(Xg) | np.isinf(Xg), axis=1))\n",
        "    Xg_clean = Xg[mask_keep_global]\n",
        "    yg_clean = yg[mask_keep_global]\n",
        "\n",
        "    if len(yg_clean) < max(10, Xg_clean.shape[1] + 1):\n",
        "        print(f\"Skipping final refit for candidate {name}: insufficient rows after global cleaning ({len(yg_clean)} rows).\")\n",
        "        models_summary[name] = summary\n",
        "        continue\n",
        "\n",
        "    rs_full = RandomizedSearchCV(estimator=est,\n",
        "                                 param_distributions=param_dist,\n",
        "                                 n_iter=24,\n",
        "                                 cv=inner_cv,\n",
        "                                 scoring='neg_root_mean_squared_error',\n",
        "                                 n_jobs=-1,\n",
        "                                 random_state=RANDOM_STATE,\n",
        "                                 error_score='raise')\n",
        "    t0 = perf_counter(); rs_full.fit(Xg_clean, yg_clean); t1 = perf_counter()\n",
        "    summary['best_params_full'] = rs_full.best_params_\n",
        "    summary['refit_time_seconds'] = float(t1 - t0)\n",
        "    final_model = rs_full.best_estimator_\n",
        "    model_path = MODELS_DIR / f'{name}_final.joblib'\n",
        "    joblib.dump(final_model, model_path)\n",
        "    summary['model_artifact'] = str(model_path)\n",
        "    preds_full = final_model.predict(Xg_clean)\n",
        "    preds_df = pd.DataFrame({'y_true': yg_clean, 'y_pred': preds_full})\n",
        "    preds_path = OUTPUTS_DIR / f'{name}_preds_full.csv'\n",
        "    preds_df.to_csv(preds_path, index=False)\n",
        "    summary['preds_artifact'] = str(preds_path)\n",
        "\n",
        "    artifact_manifest = {\n",
        "        'model_artifact': str(model_path),\n",
        "        'preds_artifact': str(preds_path),\n",
        "        'model_checksum': checksum_file(model_path) if model_path.exists() else None,\n",
        "        'preds_checksum': checksum_file(preds_path) if preds_path.exists() else None,\n",
        "        'timestamp': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime())\n",
        "    }\n",
        "    write_json_atomic(artifact_manifest, OUTPUTS_DIR / f'{name}_artifact_manifest.json')\n",
        "\n",
        "    models_summary[name] = summary\n",
        "    print('Completed candidate:', name)\n",
        "\n",
        "models_summary['run_info'] = {'timestamp': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime()), 'git_sha': (subprocess.check_output(['git','rev-parse','HEAD']).decode().strip() if Path('.git').exists() else ''), 'random_state': RANDOM_STATE}\n",
        "write_json_atomic(models_summary, OUTPUTS_DIR / 'models_summary.json')\n",
        "run_prov = capture_run_provenance(OUTPUTS_DIR / 'run_provenance.json')\n",
        "print('Wrote outputs/models_summary.json and outputs/run_provenance.json')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 4b. Confirm model outputs and artifacts (quick checks) and generate any missing artifacts\n",
        "import json\n",
        "print('models_summary exists:', (OUTPUTS_DIR/'models_summary.json').exists())\n",
        "if (OUTPUTS_DIR/'models_summary.json').exists():\n",
        "    ms = json.load(open(OUTPUTS_DIR/'models_summary.json'))\n",
        "    print('Models in summary:', [k for k in ms.keys() if k!='run_info'])\n",
        "else:\n",
        "    ms = {}\n",
        "\n",
        "# Ensure per-model preds and manifests exist; if not, try to create them from saved final models\n",
        "for m in [k for k in ms.keys() if k!='run_info']:\n",
        "    preds_path = OUTPUTS_DIR / f'{m}_preds_full.csv'\n",
        "    manifest_path = OUTPUTS_DIR / f'{m}_artifact_manifest.json'\n",
        "    model_path = MODELS_DIR / f'{m}_final.joblib'\n",
        "    if not preds_path.exists() and model_path.exists():\n",
        "        try:\n",
        "            model = joblib.load(model_path)\n",
        "            Xg = np.asarray(X_full)\n",
        "            yg = np.asarray(y_full)\n",
        "            preds = model.predict(Xg)\n",
        "            pd.DataFrame({'y_true': yg, 'y_pred': preds}).to_csv(preds_path, index=False)\n",
        "            print(f'Wrote missing preds for {m} to {preds_path}')\n",
        "        except Exception as e:\n",
        "            print('Could not write preds for', m, e)\n",
        "    if not manifest_path.exists() and model_path.exists():\n",
        "        try:\n",
        "            manifest = {\n",
        "                'model_artifact': str(model_path),\n",
        "                'model_checksum': checksum_file(model_path),\n",
        "                'timestamp': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime())\n",
        "            }\n",
        "            write_json_atomic(manifest, manifest_path)\n",
        "            print(f'Wrote artifact manifest for {m}')\n",
        "        except Exception as e:\n",
        "            print('Could not write manifest for', m, e)\n",
        "\n",
        "# Hyperparameter summary CSV\n",
        "try:\n",
        "    rows = []\n",
        "    for k,v in ms.items():\n",
        "        if k == 'run_info':\n",
        "            continue\n",
        "        rows.append({'model': k, 'cv_rmse_mean': v.get('cv_rmse_mean'), 'best_params_full': json.dumps(v.get('best_params_full', {}))})\n",
        "    if rows:\n",
        "        pd.DataFrame(rows).to_csv(OUTPUTS_DIR / 'hyperparam_summary_top.csv', index=False)\n",
        "        print('Wrote hyperparam_summary_top.csv')\n",
        "except Exception as e:\n",
        "    print('Hyperparam summary creation failed:', e)\n",
        "\n",
        "# SHAP CSV exports and ensure SHAP PNGs exist (if SHAP artifacts were created earlier, export CSVs if missing)\n",
        "try:\n",
        "    import shap, matplotlib.pyplot as plt\n",
        "    plt.ioff()\n",
        "    rng = np.random.RandomState(RANDOM_STATE)\n",
        "    for m in ['random_forest','lightgbm','elasticnet','mlp_pytorch']:\n",
        "        shap_png = OUTPUTS_DIR / f'shap_summary_{m}.png'\n",
        "        shap_csv = OUTPUTS_DIR / f'shap_values_{m}_sample.csv'\n",
        "        model_path = MODELS_DIR / f'{m}_final.joblib'\n",
        "        if model_path.exists() and not shap_png.exists():\n",
        "            try:\n",
        "                model = joblib.load(model_path)\n",
        "                Xg = np.asarray(X_full)\n",
        "                n_sample = min(SHAP_CONFIG.get('tree_sample', 500), Xg.shape[0])\n",
        "                sample_idx = rng.choice(Xg.shape[0], size=n_sample, replace=False)\n",
        "                X_sample = Xg[sample_idx]\n",
        "                if m in ['random_forest','lightgbm']:\n",
        "                    expl = shap.TreeExplainer(model)\n",
        "                    sv = expl.shap_values(X_sample)\n",
        "                elif m == 'elasticnet':\n",
        "                    expl = shap.LinearExplainer(model, Xg, feature_perturbation='interventional')\n",
        "                    sv = expl.shap_values(X_sample)\n",
        "                else:\n",
        "                    try:\n",
        "                        def predict_fn(x):\n",
        "                            return model.predict(x)\n",
        "                        bg = Xg[rng.choice(Xg.shape[0], size=min(50, Xg.shape[0]), replace=False)]\n",
        "                        expl = shap.KernelExplainer(predict_fn, bg)\n",
        "                        sv = expl.shap_values(X_sample, nsamples=SHAP_CONFIG.get('kernel_nsamples', 100))\n",
        "                    except Exception:\n",
        "                        sv = None\n",
        "                if sv is not None:\n",
        "                    try:\n",
        "                        shap.summary_plot(sv, X_sample, show=False)\n",
        "                        plt.tight_layout(); plt.savefig(shap_png, dpi=200, bbox_inches='tight'); plt.close()\n",
        "                        sv_df = pd.DataFrame(sv, columns=[f'feat_{i}' for i in range(X_sample.shape[1])])\n",
        "                        sv_df['sample_index'] = sample_idx\n",
        "                        sv_df.to_csv(shap_csv, index=False)\n",
        "                        print(f'Wrote SHAP artifacts for {m}')\n",
        "                    except Exception as e:\n",
        "                        print('Could not save SHAP artifacts for', m, e)\n",
        "            except Exception as e:\n",
        "                print('SHAP generation failed for', m, e)\n",
        "except Exception as e:\n",
        "    print('SHAP exports skipped (shap not available or failed):', e)\n",
        "\n",
        "# Residual diagnostics: save residual hist and resid vs pred for each model preds file\n",
        "try:\n",
        "    import matplotlib.pyplot as plt\n",
        "    import seaborn as sns\n",
        "    sns.set(style='whitegrid')\n",
        "    for k in [k for k in ms.keys() if k!='run_info']:\n",
        "        preds_path = OUTPUTS_DIR / f'{k}_preds_full.csv'\n",
        "        if preds_path.exists():\n",
        "            dfp = pd.read_csv(preds_path)\n",
        "            dfp['resid'] = dfp['y_true'] - dfp['y_pred']\n",
        "            fig, ax = plt.subplots(1,2, figsize=(12,4))\n",
        "            sns.histplot(dfp['resid'], kde=True, ax=ax[0])\n",
        "            ax[0].set_title('Residuals distribution')\n",
        "            sns.scatterplot(x=dfp['y_pred'], y=dfp['resid'], alpha=0.5, ax=ax[1])\n",
        "            ax[1].axhline(0, color='red', linestyle='--')\n",
        "            ax[1].set_xlabel('Predicted'); ax[1].set_ylabel('Residual')\n",
        "            fig.savefig(FIG_DIR / f'resid_{k}.png', dpi=200, bbox_inches='tight')\n",
        "            plt.close(fig)\n",
        "            print('Saved residual diagnostics for', k)\n",
        "except Exception as e:\n",
        "    print('Residual diagnostics generation failed:', e)\n",
        "\n",
        "print('Artifact generation and checks complete. List outputs:')\n",
        "for p in sorted(list(OUTPUTS_DIR.glob('*'))):\n",
        "    print(' -', p.name)\n"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 5. SHAP analyses (guarded) - already exported to CSVs above; this cell is a safe re-run if needed\n",
        "try:\n",
        "    import shap\n",
        "    print('SHAP available; SHAP CSVs and PNGs are in outputs/ if generation succeeded.')\n",
        "except Exception:\n",
        "    print('SHAP not available in this environment; SHAP steps skipped.')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 6. Conformal prediction (split-conformal) for regression intervals and calibration stats\n",
        "from sklearn.model_selection import train_test_split\n",
        "\n",
        "def split_conformal_interval(model, X_cal, y_cal, X_test, alpha=0.1):\n",
        "    y_cal_pred = model.predict(X_cal)\n",
        "    resid = np.abs(y_cal - y_cal_pred)\n",
        "    q = np.quantile(resid, 1 - alpha)\n",
        "    y_test_pred = model.predict(X_test)\n",
        "    lower = y_test_pred - q\n",
        "    upper = y_test_pred + q\n",
        "    return lower, upper, q\n",
        "\n",
        "for name, info in list(models_summary.items()):\n",
        "    if name == 'run_info':\n",
        "        continue\n",
        "    model_path = Path(info.get('model_artifact',''))\n",
        "    if model_path.exists():\n",
        "        model = joblib.load(model_path)\n",
        "        X_tr, X_cal, y_tr, y_cal = train_test_split(X_full, y_full, test_size=0.2, random_state=RANDOM_STATE)\n",
        "        lower, upper, q = split_conformal_interval(model, X_cal, y_cal, X_cal, alpha=0.1)\n",
        "        models_summary[name]['conformal_q_90'] = float(q)\n",
        "    else:\n",
        "        models_summary[name]['conformal_q_90'] = None\n",
        "\n",
        "write_json_atomic(models_summary, OUTPUTS_DIR / 'models_summary.json')\n",
        "print('Updated models_summary.json with conformal quantiles')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 7. Resource snapshot (CPU/GPU) and write to outputs\n",
        "def resource_snapshot():\n",
        "    snap = {}\n",
        "    if PSUTIL_AVAILABLE:\n",
        "        snap['cpu_percent'] = psutil.cpu_percent(interval=1)\n",
        "        snap['virtual_memory'] = psutil.virtual_memory()._asdict()\n",
        "    else:\n",
        "        snap['cpu_percent'] = None\n",
        "        snap['virtual_memory'] = None\n",
        "    if TORCH_AVAILABLE:\n",
        "        try:\n",
        "            snap['torch_version'] = torch.__version__\n",
        "            snap['cuda_available'] = torch.cuda.is_available()\n",
        "            snap['cuda_device_count'] = torch.cuda.device_count()\n",
        "            if torch.cuda.is_available():\n",
        "                try:\n",
        "                    snap['cuda_device_name'] = torch.cuda.get_device_name(torch.cuda.current_device())\n",
        "                except Exception:\n",
        "                    snap['cuda_device_name'] = ''\n",
        "        except Exception:\n",
        "            pass\n",
        "    return snap\n",
        "\n",
        "write_json_atomic({'resource_snapshot': resource_snapshot()}, OUTPUTS_DIR / 'resource_snapshot.json')\n",
        "print('Wrote resource snapshot to outputs/resource_snapshot.json')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 8. Optional stacking (disabled by default)\n",
        "DO_STACKING = False\n",
        "if DO_STACKING:\n",
        "    from sklearn.linear_model import Ridge\n",
        "    base_models = [k for k in models_summary.keys() if k not in ['run_info']]\n",
        "    oof_preds = pd.DataFrame(index=range(X_full.shape[0]))\n",
        "    for m in base_models:\n",
        "        model = joblib.load(MODELS_DIR / f'{m}_final.joblib')\n",
        "        oof_preds[m] = model.predict(X_full)\n",
        "    meta = Ridge()\n",
        "    meta.fit(oof_preds, y_full)\n",
        "    joblib.dump(meta, MODELS_DIR / 'stacked_meta_final.joblib')\n",
        "    models_summary['stacked'] = {'model_artifact': str(MODELS_DIR / 'stacked_meta_final.joblib')}\n",
        "    write_json_atomic(models_summary, OUTPUTS_DIR / 'models_summary.json')\n",
        "    print('Stacking completed and saved')\n",
        "else:\n",
        "    print('Stacking disabled (DO_STACKING=False).')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 9. Save README.md snippet for reproducibility (if not present)\n",
        "readme_path = ROOT / 'README.md'\n",
        "if not readme_path.exists():\n",
        "    readme_text = '''# County Childhood Vaccination Uptake\n\nThis repository contains the analysis notebook `analysis_updated_full.ipynb` and reports in `reports/`.\n\n**How to run**\n\n1. Create a Python environment with required packages (scikit-learn, pandas, numpy, joblib, shap, lightgbm optional, xgboost optional, torch optional).\n2. Place raw data files in `data_raw/` as described in the notebook.\n3. Open `analysis_updated_full.ipynb`, restart the kernel, and run all cells top→bottom.\n4. Knit the R Markdown reports in `reports/` from the project root (requires R and `rmarkdown`, `kableExtra`, `ggplot2`).\n\nArtifacts are saved to `outputs/` and models to `models/`.\n'''\n",
        "    readme_path.write_text(readme_text)\n",
        "    print('Wrote README.md')\n",
        "else:\n",
        "    print('README.md already exists; not overwritten.')"
      ]
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {},
      "source": [
        "# 10. Finalize: write models_summary.json and run provenance again\n",
        "if (OUTPUTS_DIR / 'models_summary.json').exists():\n",
        "    print('Final models_summary.json present at outputs/models_summary.json')\n",
        "else:\n",
        "    write_json_atomic(models_summary, OUTPUTS_DIR / 'models_summary.json')\n",
        "run_prov = capture_run_provenance(OUTPUTS_DIR / 'run_provenance.json')\n",
        "print('Finalization complete. Check outputs/ and models/ for artifacts.')"
      ]
    }
  ]
}